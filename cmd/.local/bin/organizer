#!/bin/bash
# Ask for a choice in organizer

choices="notes\ntodo"

question=$(echo -e "$choices" | dmenu -i)

case "$question" in
  "notes") marker ~/.organizer/notes.md ;;
  "todo") marker ~/.organizer/todo.md ;;
esac
